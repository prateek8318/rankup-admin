import { ExamsManagementContent } from "../../features/exams/ExamsManagementContent";
import PageLayout from "../../components/layout/PageLayout";

const ExamsManagement = () => {
  return (
    <PageLayout>
      <ExamsManagementContent />
    </PageLayout>
  );
};

export default ExamsManagement;
